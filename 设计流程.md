# 企业微信 AI 助手 — 设计方案

**版本**: v2.0 | **日期**: 2026-05-16 | **技术栈**: Python 3.12 + wecom-aibot-sdk-python + DeepSeek v4-pro

---

## 0. 关键决策：为什么用企业微信而不是个人微信 Hook

| 方案        | 原理               | 现状                         | 结论       |
| ----------- | ------------------ | ---------------------------- | ---------- |
| WeChatFerry | Hook 微信 Windows 客户端 | 微信 4.1.9.35 无兼容 Hook；3.9.12.17 被服务端强制升级拦截 | 不可用     |
| WeChaty     | 微信 Web 协议      | 2017 年后新号无法 Web 登录   | 不可用     |
| 企业微信    | 官方智能机器人 WebSocket | 无需公网 IP/域名，官方 API 无封号风险，仅需 BotID + Secret | **选用** |

企业微信智能机器人通过 WebSocket 长连接主动连接 `wss://openws.work.weixin.qq.com`，不需要公网 IP、HTTPS 证书或消息加解密，创建即用。

---

## 1. 系统模块

### 1.1 模块顺序与功能

| 顺序 | 模块               | 输入                     | 输出                   | 说明                                         |
| ---- | ------------------ | ------------------------ | ---------------------- | -------------------------------------------- |
| 1    | 微信接入层         | 企微 WebSocket 消息帧    | 标准化消息对象           | 基于 wecom-aibot-sdk-python，封装消息收发，msg_id 去重，流式回复 |
| 2    | 触发判断模块       | 标准化消息对象           | 布尔值 + 触发原因        | 私聊默认触发；群聊需 @机器人名或命中触发词     |
| 3    | 会话上下文管理模块 | 用户 ID + 消息文本       | 会话历史列表             | 群聊按 `group_id:user_id` 隔离上下文，deque + 超时过期清理 |
| 4    | 频率控制模块       | 用户 ID + 消息对象       | 放行 / 限流拒绝          | 滑动窗口 + 计数，防止刷屏和 API 费用爆炸      |
| 5    | AI 调用模块        | 消息文本 + 会话上下文    | AI 回复内容（流式）      | 统一入口，支持文本对话 + 多模态路由            |
| 6    | 消息回复模块       | AI 回复内容 + 目标用户   | 企微消息发送             | 文本分段发送；超长文本自动拆分                |
| 7    | 日志与异常处理模块 | 全局事件                 | 日志文件 + 兜底回复      | 记录全链路，AI 异常时返回兜底文本             |
| 8    | PUBG 数据模块      | 游戏 ID                  | 生涯/赛季/比赛统计      | PUBG 官方 API 实时拉取 + TTL 缓存            |
| 9    | 群友档案模块       | /setpubg 指令            | JSON 持久化档案          | 记录群友游戏 ID，自动注入 AI 上下文           |

### 1.2 模块职责详述

**模块 1 — 微信接入层**

```
企业微信 WebSocket (wss://openws.work.weixin.qq.com)
        │
        ├── aibot_msg_callback ──→ 消息解析 ──→ 标准化消息对象
        │                                          ├─ msg_id: 唯一ID
        │                                          ├─ from_user: 发送者 userid
        │                                          ├─ content: 文本内容
        │                                          ├─ msg_type: text / image / voice / event / ...
        │                                          ├─ is_group: 是否群聊
        │                                          ├─ group_id: 群聊时对应的 chatid（私聊 = None）
        │                                          └─ timestamp: 消息时间戳
        │
        └── aibot_recv_event  ──→ 事件解析（enter_chat 等）
```

- 基于 `wecom-aibot-sdk-python` 的 WSClient，通过 BotID + Secret 认证
- SDK 对同一条消息可能 dispatch 多次，通过 msg_id set 去重（上限 10000 条）
- 回复支持一次性发送（`send_text`）和流式增量更新（`send_stream_update`）
- 群聊自动 `@发送者`

**模块 2 — 触发判断模块**

| 场景         | 条件                          | 备注                           |
| ------------ | ----------------------------- | ------------------------------ |
| 私聊         | 始终触发                      | 除非用户被限流                  |
| 群聊         | 消息包含 `@机器人名` 或命中自定义触发词 | 触发词可配置，如 `小助手`、`AI` |
| 进入会话事件 | 忽略                          | enter_chat 事件不响应           |

触发逻辑内置于接入层，通过区分 `aibot_msg_callback` 和 `aibot_recv_event` cmd 实现。

**模块 3 — 会话上下文管理模块**

- 每个用户（私聊）或群内每人（群聊）维护独立的对话历史
- key 格式：私聊 = `user_id`，群聊 = `group_id:user_id`（避免串话）
- 上下文窗口：最近 N 轮对话（默认 10 轮，可配置）
- 超时机制：超过 T 分钟无新消息，清除该用户的上下文（默认 30 分钟）
- 存储方式：内存 dict → `{key: Session(deque, last_active)}`，不落盘

**模块 4 — 频率控制模块**

- 每用户滑动窗口限制：窗口 W 秒内最多 N 条（默认 W=60s, N=10）
- 超过限制后回复一条提示（"消息太频繁，请稍后再试"），然后丢弃后续消息直到窗口重置
- 全局 QPS 限制（默认 5 QPS）

**模块 5 — AI 调用模块**

| 用户意图          | 调用方式           | 说明                            |
| ----------------- | ------------------ | ------------------------------- |
| 普通文本对话      | LLM Chat API       | 携带上下文 + PUBG 数据，流式输出 |
| 图片生成          | DALL·E API         | 预留接口（`generate_image`）     |
| 语音消息识别      | Whisper API        | 预留接口（`transcribe_audio`）   |
| TTS 语音回复      | TTS API            | 预留接口（`text_to_speech`）     |
| 快捷指令          | 本地逻辑匹配       | `/help`、`/reset`、`/status`、`/setpubg`、`/pubg`、`/pubglist` |

- 流式输出：每累积 50 字符发送一次流式更新，最后以 finish=True 结束
- AI provider 为 DeepSeek（OpenAI 兼容接口，`api.deepseek.com`）

快捷指令列表：
- `/help` — 显示帮助信息
- `/reset` — 清除当前会话上下文
- `/status` — 显示当前会话状态（消息数、上下文长度、限流状态等）
- `/setpubg <游戏ID> [区服]` — 设置 PUBG 游戏 ID
- `/pubg` — 查看自己的 PUBG 档案
- `/pubglist` — 查看本群所有人的 PUBG 档案

**模块 6 — 消息回复模块**

- 文本：直接调用 adapter 发送
- 超长文本自动按 4000 字符分段发送
- 异常兜底回复："我没听懂你说的"

**模块 7 — 日志与异常处理模块**

日志内容：
- 每条消息的收发记录（脱敏后）
- AI 调用耗时
- 异常堆栈

输出：控制台（强制 UTF-8）+ 文件（`logs/app.log`）

异常处理策略：
| 异常类型           | 处理方式                       | 用户感知                     |
| ------------------ | ------------------------------ | ---------------------------- |
| AI API 超时/5xx    | 直接返回兜底回复               | "我没听懂你说的"             |
| AI API 4xx（鉴权） | 不再重试，记录日志             | 同上                         |
| 企微消息发送失败   | 记录日志                       | 无感知（静默失败）           |

**模块 8 — PUBG 数据模块**

- `pubg_api.py`：PUBG 官方 API 客户端（`developer.pubg.com`），httpx 异步请求
- `pubg_cache.py`：TTL 缓存（默认 5 分钟），避免 API 限流（10 次/分钟）
- 数据内容：生涯 KD/击杀/吃鸡/对战场次/TOP10 + 本赛季数据 + 最近 5 场比赛
- match ID 从玩家 `relationships.matches.data` 提取，逐个查询 match 详情

**模块 9 — 群友档案模块**

- JSON 持久化存储（`data/profiles.json`），线程安全
- key = `group_id:user_id`，群聊内每人独立档案
- 每次消息到达时自动从档案读取游戏 ID → 查 PUBG API → 注入 AI 上下文

---

## 2. 数据流（完整链路）

```
用户发送消息
    │
    ▼
[企业微信 WebSocket] ── 接收消息帧，转为标准化消息对象
    │
    ▼
[msg_id 去重] ── 重复消息 ──▶ 跳过
    │
    ▼
[事件过滤] ── enter_chat ──▶ 忽略
    │
    ▼
[触发判断] ── 不触发 ──▶ 忽略
    │ 触发
    ▼
[频率控制] ── 超限 ──▶ 返回限流提示
    │ 放行
    ▼
[快捷指令匹配] ── 命中 ──▶ 直接回复
    │ 未命中
    ▼
[会话上下文] ── 加载该用户/群的历史
    │
    ▼
[群友档案] ── 查 game_id ──▶ [PUBG 缓存] ── miss ──▶ [PUBG API]
    │                              │  hit                  │
    │                              ▼                       ▼
    │                         缓存命中              获取生涯+赛季+比赛
    │                              │                       │
    │                              ▼                       ▼
    │                         注入战绩到 system prompt
    │
    ▼
[AI 调用] ── 携带 system prompt + 会话历史 + PUBG 数据 ──▶ 流式回复
    │
    ├── 正常 ──▶ [消息回复] ──▶ 企业微信客户端
    │
    └── 异常 ──▶ [异常处理] ──▶ 兜底回复 ──▶ 企业微信客户端
                                       │
                                      ▼
                               [日志记录]（全链路）
```

---

## 3. 项目结构（实际）

```
WeChat AI/
├── main.py                    # 入口：asyncio 驱动，组装所有模块
├── config.yaml                # 配置文件（API key、触发词、限流参数、bot_persona）
├── .env                       # 密钥（不进 git）
├── requirements.txt           # wecom-aibot-sdk-python, openai, pyyaml
├── utils/
│   └── config.py              # 配置加载 + .env + ${VAR} 环境变量解析
├── modules/
│   ├── wecom_adapter.py       # 模块 1：企微 WebSocket 接入层（含消息解析/去重/流式发送）
│   ├── session.py             # 模块 3：会话上下文管理
│   ├── rate_limiter.py        # 模块 4：频率控制
│   ├── ai_engine.py           # 模块 5：AI 调用（chat + chat_stream + 多模态接口）
│   ├── reply.py               # 模块 6：消息回复
│   ├── logger.py              # 模块 7：日志与异常处理
│   ├── profile_store.py       # 模块 9：群友档案 JSON 持久化
│   ├── pubg_api.py            # 模块 8：PUBG 官方 API 客户端
│   └── pubg_cache.py          # 模块 8：PUBG API TTL 缓存
├── commands/
│   └── builtin.py             # 快捷指令注册与分发
├── data/
│   └── profiles.json          # 群友档案（运行中生成）
├── logs/                      # 日志输出目录
└── temp/                      # 临时文件目录
```

---

## 4. 实现优先级

| 优先级 | 内容                     | 状态     |
| ------ | ------------------------ | -------- |
| P0     | 模块 1+5+6+7             | 已完成   |
| P1     | 模块 3+4                 | 已完成   |
| P2     | 模块 8+9（PUBG 数据+档案） | 已完成 |
| P3     | 模块 5 多模态（图片/语音/TTS） | 预留接口 |

---

## 5. 运行说明

```bash
# 1. 配置密钥
# 在企业微信客户端 → 工作台 → 智能机器人 → API 模式创建
# 将 BotID、Secret、AI_API_KEY 填入 .env

# 2. 安装依赖
pip install -r requirements.txt

# 3. 启动
python main.py
```

- 运行环境：Git Bash，`.bashrc` 已配置 `PYTHONIOENCODING=utf-8` 和 PATH
- Python 路径：`D:\FFDevelop\Python312`
- 无 webhook 回调地址、公网 IP、HTTPS 证书需求
