# 版本迭代

## v0.1 — 项目启动（2026-05 上旬）

**起因**：想给游戏群搞个 AI 助手，初始方案用个人微信 Hook（wcferry），微信版本 4.1.9.35 无兼容方案，降级到 3.9.12.17 被服务端强制升级拦截。

**决策**：转向企业微信智能机器人 WebSocket 长连接，无需公网 IP/域名，官方 API 无封号风险。

**技术选型**：Python 3.12 + `wecom-aibot-sdk-python` + DeepSeek v4-pro

---

## v0.2 — 核心联调（2026-05 上旬）

**解决的问题**：

| 问题 | 原因 | 修复 |
|------|------|------|
| Git Bash 中文乱码 | Windows 编码 | `PYTHONIOENCODING=utf-8` + stdout.reconfigure |
| 机器人不回复 | cmd 名错误 | `aibot_recv_msg` → `aibot_msg_callback` |
| 回复两次 | SDK 同消息 dispatch 多次 | msg_id 去重 set |
| 连接 11 秒断开 | connect_async 非阻塞 | asyncio.Event 保活 |
| Python 找不到 | Windows Store stub 劫持 | 关闭应用执行别名 + 迁移 Python 到 D 盘 |

---

## v0.3 — 体验优化（2026-05 中旬）

**流式输出**：AI 回复改为流式，每累积 50 字推一次更新，不再等全文生成完。

**上下文隔离**：群聊 session key 从 `group_id` 改为 `group_id:user_id`，每个人的对话上下文独立。

**人格可配置**：机器人设定从硬编码移到 `config.yaml` 的 `bot_persona`，修改人格无需改代码。

---

## v0.4 — PUBG 人格（2026-05 中旬）

**起因**：用户反馈机器人没有差异化，跟原生创建的没区别。

**内容**：将机器人设定为 PUBG 退役选手「老六」，精通 PUBG，鄙视非 PUBG 游戏。人格写在 `config.yaml` 由 DeepSeek AI 驱动。

---

## v0.5 — 群友档案（2026-05-16）

**起因**：用户要求让机器人有记忆能力，记住群友游戏数据。

**新增**：
- `modules/profile_store.py` — JSON 持久化档案存储，线程安全
- `/setpubg`、`/pubg`、`/pubglist` 命令
- 用户档案自动注入 AI 上下文

**设计**：key = `group_id:user_id`，群聊内每人独立档案，自由文本格式。

---

## v0.6 — PUBG 官方数据接入（2026-05-16）

**起因**：用户提出「只设一次游戏 ID，数据从官方 API 实时获取」。

**新增**：
- `modules/pubg_api.py` — PUBG 官方 API 客户端（`developer.pubg.com`），异步拉取生涯数据
- `modules/pubg_cache.py` — TTL 缓存（5 分钟），避免 API 限流（10次/分钟）

**改动**：
- `/setpubg` 改为只设置游戏 ID（可选区服，默认 steam）
- `main.py` 每次消息到达时实时查战绩，注入 AI 上下文

**API 数据**：生涯 KD、总击杀、吃鸡次数、对战场次、TOP10、总伤害

**依赖**：`httpx`（已随 openai 安装）

---

## v0.6.1 — PUBG 赛季 + 比赛记录（2026-05-16）

**起因**：只有生涯总数据不够，用户需要本赛季数据 + 最近战绩。

**改动**：
- `PubgStats` 新增 `season_*` 字段和 `recent_matches` 列表
- `PubgApiClient` 新增 `_get_current_season_id`、`_get_match_summary`
- match ID 从玩家查询的 `relationships.matches` 提取，逐个查 match 详情
- `format()` 输出分三段：生涯 / 本赛季 / 最近5场比赛（地图、模式、排名、击杀、存活时间）
- match ID 获取来源修复：原从 `gameModeStats.*.matchesPlayed` 取（不存在），改为从玩家 `relationships.matches.data` 取

---

## v0.7 — PUBG 按需查询 + 新用户引导（2026-05-16）

**起因**：三个使用问题——① 每次 @都调 PUBG API，浪费 token 和 API 配额；② 新用户不知道如何绑定 ID；③ 已绑定用户想修改 ID 没有提示。

**决策**：靠 AI 理解意图来做引导，不写硬编码的 if-else。三点讨论后一致通过。

**改动**：

| 文件 | 改动 |
|------|------|
| `config.yaml` | system prompt 新增「功能引导」规则：用户想查战绩但没绑定 ID → 引导 /setpubg；想修改 ID → 告知直接覆盖；附带战绩数据时才基于数据回复 |
| `main.py` | 新增 `_PUBG_STATS_KEYWORDS`（17 个关键词：战绩、KD、吃鸡、数据、击杀等）+ `_wants_pubg_stats()` 检测函数；PUBG API 查询从无条件注入改为 `if _wants_pubg_stats(content)` 按需触发 |
| `commands/builtin.py` | `/pubg` 有档案时返回 None，落入 AI 流程触发实时查询；无档案时返回引导文本；`/help` 描述同步更新 |
| `设计流程.md` | 从 WeChatFerry 方案同步为实际的企业微信方案，补全模块 8/9，标注实现优先级状态 |

---

## v0.7.1 — 测试修复（2026-05-16）

**起因**：v0.7 灰度测试发现三个 bug。

**问题与修复**：

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | `/setpubg lrhe5847 同时查一下战绩` 把"同时查一下战绩"当成区服 | `_setpubg` 无 shard 校验 | 新增 `VALID_SHARDS` 白名单，不合法时忽略、默认 steam 并返回 None 让消息落入 AI 流程 |
| 2 | AI 查战绩时说"刚绑的号还是白板"，捏造数据 | API 无数据时 system prompt 留空，AI 瞎编 | 无数据时注入明确的 system 消息（"API 未返回数据"或"用户未绑定 ID"），AI 如实告知 |
| 3 | 某用户 @机器人发指令，机器人静默无回复 | 疑似 SDK `reply_stream` 静默失败，日志级别为 exception/不显眼 | `send_text` 和 `send_stream_update` 增加 info 级成功日志 + warning 级失败日志，均附带 to=user_id |

**改动文件**：`commands/builtin.py`、`main.py`、`modules/wecom_adapter.py`、`config.yaml`

---

## v0.7.2 — 修复 @mention 导致指令不执行（2026-05-16）

**起因**：v0.7.1 测试 —— `/setpubg` 后机器人回复"绑上了"，但 `/pubg` 报"未绑定"。查日志发现 `/setpubg` 根本没执行。

**根因**：企微把用户消息里的 `@机器人名` 留在了 content 中（如 `@小 p  /setpubg GAOYIFEIOPA`）。`dispatch()` 用 `split()[0]` 取命令，拿到的是 `@小` 而非 `/setpubg`，指令从未匹配。AI 照着 system prompt 演了一出"绑上了"的戏，但 profile 并未保存。

**修复**：`dispatch()` 在解析命令前，用 `text.find("/")` 定位第一个 `/` 指令的位置，跳过前面的 @mention。`_wants_pubg_stats` 用子串匹配不受影响。

**改动文件**：`commands/builtin.py`（`dispatch`）、`main.py`（版本号、调试日志）

**附加清理**：删除 `modules/trigger.py`（WeChatFerry 旧方案残留，从未被 import）；移除 `config.yaml` 的 `trigger` 段（企微平台层处理 @触发，代码无需关心）。

---

## v0.7.3 — 代码清理（2026-05-17）

**起因**：Code review 发现死代码和冗余配置。

| # | 文件 | 清理内容 |
|---|------|---------|
| 1 | `modules/wechat_adapter.py` | 删除整个文件（172行）。WeChatFerry 适配器，从未被 import，项目已全面使用 `wecom_adapter.py` |
| 2 | `requirements.txt` | 补充 `httpx>=0.27.0`。`pubg_api.py` 直接 import httpx，之前靠 openai 间接依赖 |
| 3 | `main.py` | 删除未使用的 `ai_config` 局部变量 |
| 4 | `config.yaml` | 删除 `ai.provider`（无代码读取）和 `bot_persona.name`（无代码读取） |

**保留的预留接口**（P3 多模态，设计文档明确标注，暂不删）：`ai_engine.py` 的 `chat()`、`generate_image()`、`transcribe_audio()`、`text_to_speech()`

---

## v0.7.4 — 项目文档与 Git（2026-05-17）

**起因**：项目首次 Git 管理 + 撰写 README。

**内容**：

| 操作 | 说明 |
|------|------|
| `git init` + 首次提交 | 20 个文件、1803 行，推送至 GitHub |
| `.gitignore` | 排除 `.env`、`logs/`、`temp/`、`data/`、`__pycache__/`、`.claude/`、`memory/`、测试文件、聊天记录 |
| `README.md` | 项目说明：快速开始、功能、项目结构、技术栈 |
| `.env.example` | 环境变量模板 |

---

## v0.7.5 — 开发协作总结（2026-05-17）

**起因**：项目收官，总结开发过程中的问题与有效习惯。

**输出**：`开发协作总结.md`（本地文件，不进 git）。记录 7 个典型问题（设计文档脱节、遗留代码不清理、无测试就群测、不跑回归、谎言配置、info 日志太省、间接依赖未声明）和 5 个有效习惯（讨论再改、先出方案再写代码、版本迭代记录、设计文档反向更新、自动化测试）。
